{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Python Debugger: Module with Arguments",
            "type": "debugpy",
            "request": "launch",
            "module": "rhythmsync.main",
            "console": "integratedTerminal",
            "args": "${command:pickArgs}"
        }
    ]
}